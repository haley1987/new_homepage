<section class="hw-section hw-product-explorer" aria-label="{{ section.settings.accessibility_label | escape }}">
  <div class="hw-shell hw-product-explorer__header">
    <div>
      {%- if section.settings.eyebrow != blank -%}<p class="hw-kicker">{{ section.settings.eyebrow | escape }}</p>{%- endif -%}
      {%- if section.settings.heading != blank -%}<h2 class="hw-title hw-title--medium">{{ section.settings.heading | escape }}</h2>{%- endif -%}
    </div>
    {%- if section.settings.copy != blank -%}<div class="hw-copy">{{ section.settings.copy }}</div>{%- endif -%}
  </div>

  <div class="hw-product-explorer__grid">
    {%- for block in section.blocks -%}
      {%- liquid
        assign card_link = block.settings.manual_link
        assign card_sizes = '(min-width: 990px) 25vw, (min-width: 750px) 50vw, 50vw'
        if forloop.index <= 2
          assign card_sizes = '(min-width: 990px) 50vw, 100vw'
        endif
        if card_link == blank and block.settings.product != blank
          assign card_link = block.settings.product.url
        endif
        if card_link == blank and block.settings.collection != blank
          assign card_link = block.settings.collection.url
        endif
        assign card_image = block.settings.image
        if card_image == blank and block.settings.product != blank
          assign card_image = block.settings.product.featured_image
        endif
        if card_image == blank and block.settings.collection != blank
          assign card_image = block.settings.collection.featured_image
        endif
      -%}
      <article class="hw-product-card{% if forloop.index <= 2 %} hw-product-card--featured{% else %} hw-product-card--supporting{% endif %}" style="--hw-card-fit: {{ block.settings.image_fit }}; --hw-card-position: {{ block.settings.image_position }};" {{ block.shopify_attributes }}>
        <div class="hw-product-card__media">
          {%- if card_image != blank -%}
            {%- if forloop.index <= 2 -%}
              {{ card_image | image_url: width: 1800 | image_tag: class: 'hw-product-card__image hw-product-card__image--backdrop', widths: '400, 550, 750, 1000, 1200, 1400, 1800', sizes: card_sizes, loading: 'lazy', alt: '' }}
              {{ card_image | image_url: width: 1800 | image_tag: class: 'hw-product-card__image hw-product-card__image--foreground', widths: '400, 550, 750, 1000, 1200, 1400, 1800', sizes: card_sizes, loading: 'lazy' }}
            {%- else -%}
              {{ card_image | image_url: width: 1800 | image_tag: class: 'hw-product-card__image', widths: '400, 550, 750, 1000, 1200, 1400, 1800', sizes: card_sizes, loading: 'lazy' }}
            {%- endif -%}
          {%- else -%}
            <div class="hw-product-card__placeholder" data-image-purpose="{{ block.settings.title | escape }} PRODUCT IMAGE">
              <span>{{ block.settings.title | escape }} PRODUCT IMAGE</span>
            </div>
          {%- endif -%}
        </div>
        {%- if block.settings.badge != blank -%}<span class="hw-product-card__badge">{{ block.settings.badge | escape }}</span>{%- endif -%}
        <div class="hw-product-card__content">
          <h3 class="hw-product-card__title">{{ block.settings.title | escape }}</h3>
          {%- if block.settings.category != blank -%}<p class="hw-product-card__category">{{ block.settings.category | escape }}</p>{%- endif -%}
          {%- if block.settings.fit != blank -%}<p class="hw-product-card__fit">{{ block.settings.fit | escape }}</p>{%- endif -%}
          <span class="hw-product-card__cta">Explore {{ block.settings.title | escape }} <span aria-hidden="true">→</span></span>
        </div>
        {%- if card_link != blank -%}<a class="hw-card-link" href="{{ card_link }}"><span class="visually-hidden">Explore {{ block.settings.title | escape }}</span></a>{%- endif -%}
      </article>
    {%- endfor -%}
  </div>

  <div class="hw-shell hw-product-explorer__footer">
    <div class="hw-actions">
      {%- if section.settings.primary_label != blank and section.settings.primary_link != blank -%}<a class="hw-button hw-button--solid" href="{{ section.settings.primary_link }}">{{ section.settings.primary_label | escape }}</a>{%- endif -%}
      {%- if section.settings.secondary_label != blank and section.settings.secondary_link != blank -%}<a class="hw-button" href="{{ section.settings.secondary_link }}">{{ section.settings.secondary_label | escape }}</a>{%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Haywood product explorer",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Explore the lineup" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Built around the way you play." },
    { "type": "richtext", "id": "copy", "label": "Supporting copy", "default": "<p>Different shapes. Different flights. The same commitment to feel and precision.</p>" },
    { "type": "text", "id": "primary_label", "label": "Primary CTA label", "default": "Shop All Clubs" },
    { "type": "url", "id": "primary_link", "label": "Primary CTA link" },
    { "type": "text", "id": "secondary_label", "label": "Secondary CTA label", "default": "Find My Fit" },
    { "type": "url", "id": "secondary_link", "label": "Secondary CTA link" },
    { "type": "text", "id": "accessibility_label", "label": "Accessibility label", "default": "Explore Haywood golf clubs" }
  ],
  "blocks": [
    { "type": "card", "name": "Product family", "settings": [
      { "type": "text", "id": "title", "label": "Title", "default": "CB2" },
      { "type": "text", "id": "category", "label": "Category", "default": "Players cavity" },
      { "type": "text", "id": "badge", "label": "Badge", "info": "Optional short label, such as New" },
      { "type": "textarea", "id": "fit", "label": "Golfer-fit context", "default": "Forgiveness and control without losing the feedback better players expect." },
      { "type": "image_picker", "id": "image", "label": "Image" },
      { "type": "select", "id": "image_fit", "label": "Image fit", "options": [
        { "value": "cover", "label": "Crop to fill" }, { "value": "contain", "label": "Show complete image" }
      ], "default": "cover" },
      { "type": "select", "id": "image_position", "label": "Image position", "options": [
        { "value": "left top", "label": "Left top" }, { "value": "center top", "label": "Center top" }, { "value": "right top", "label": "Right top" },
        { "value": "left center", "label": "Left center" }, { "value": "center center", "label": "Center" }, { "value": "right center", "label": "Right center" },
        { "value": "left bottom", "label": "Left bottom" }, { "value": "center bottom", "label": "Center bottom" }, { "value": "right bottom", "label": "Right bottom" }
      ], "default": "center center" },
      { "type": "product", "id": "product", "label": "Product" },
      { "type": "collection", "id": "collection", "label": "Collection" },
      { "type": "url", "id": "manual_link", "label": "Manual link" }
    ] }
  ],
  "max_blocks": 8,
  "presets": [{ "name": "Haywood product explorer", "blocks": [{ "type": "card" }, { "type": "card" }, { "type": "card" }, { "type": "card" }, { "type": "card" }, { "type": "card" }] }]
}
{% endschema %}
